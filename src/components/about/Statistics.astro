<section class="statistics section">

    <div class="bg-pattern"></div>

    <div class="container">

        <div class="stats-grid">

            <div class="stat">

    <h2>

        <span
            class="counter"
            data-target="30"
        >
            0
        </span>

        <span class="suffix">+</span>

    </h2>

    <p>Years Experience</p>

</div>

<div class="stat">

    <h2>

        <span
            class="counter"
            data-target="500"
        >
            0
        </span>

        <span class="suffix">+</span>

    </h2>

    <p>Projects</p>

</div>

<div class="stat">

    <h2>

        <span
            class="counter"
            data-target="100"
        >
            0
        </span>

        <span class="suffix">+</span>

    </h2>

    <p>Happy Clients</p>

</div>

<div class="stat">

    <h2>

        <span
            class="counter"
            data-target="1995"
        >
            0
        </span>

    </h2>

    <p>Established</p>

</div>

        </div>

    </div>

</section>

<script is:inline>

const statsSection = document.querySelector(".statistics");

if (statsSection) {

    const counters = statsSection.querySelectorAll(".counter");

    let started = false;

    const observer = new IntersectionObserver((entries) => {

        if (!entries[0].isIntersecting || started) return;

        started = true;

        statsSection.classList.add("visible");

        counters.forEach((counter, index) => {

            setTimeout(() => {

                animate(counter);

            }, index * 180);

        });

    }, {
        threshold: 0.35
    });

    observer.observe(statsSection);

    function animate(counter) {

        const target = Number(counter.dataset.target);

        const duration = 1800;

        const start = performance.now();

        function update(now) {

            const progress = Math.min((now - start) / duration, 1);

            // Ease Out Cubic
            const eased = 1 - Math.pow(1 - progress, 3);

            const value = Math.floor(target * eased);

            counter.textContent = value.toLocaleString();

            if (progress < 1) {

                requestAnimationFrame(update);

            } else {

                counter.textContent = target.toLocaleString();

            }

        }

        requestAnimationFrame(update);

    }

}

</script>

<style>
.statistics{

    position:relative;

    overflow:hidden;

    background:
        radial-gradient(circle at top left,
        rgba(255,105,0,.08),
        transparent 40%),
        linear-gradient(180deg,#1c1c1c,#111);

    color:white;

}

/* -------------------------------- */
/* Background Grid */
/* -------------------------------- */

.bg-pattern{

    position:absolute;

    inset:0;

    background-image:

        linear-gradient(
            rgba(255,255,255,.04) 1px,
            transparent 1px
        ),

        linear-gradient(
            90deg,
            rgba(255,255,255,.04) 1px,
            transparent 1px
        );

    background-size:80px 80px;

    opacity:.35;

}

/* -------------------------------- */

.statistics::before{

    content:"";

    position:absolute;

    width:1200px;

    height:1200px;

    right:-450px;

    top:-450px;

    border:1px solid rgba(255,255,255,.05);

    border-radius:50%;

}

.statistics::after{

    content:"";

    position:absolute;

    width:900px;

    height:900px;

    left:-420px;

    bottom:-420px;

    border:1px solid rgba(255,255,255,.04);

    border-radius:50%;

}

.container{

    position:relative;

    z-index:2;

}

/* -------------------------------- */

.stats-grid{

    display:grid;

    grid-template-columns:repeat(4,1fr);

    gap:32px;

}

/* -------------------------------- */

.stat{

    position:relative;

    padding:55px 35px;

    text-align:center;

    overflow:hidden;

    border-radius:24px;

    border:1px solid rgba(255,255,255,.08);

    background:rgba(255,255,255,.04);

    backdrop-filter:blur(18px);

    -webkit-backdrop-filter:blur(18px);

    transition:

        transform .45s ease,

        border-color .45s ease,

        background .45s ease,

        opacity .8s ease,

        box-shadow .45s ease;

    opacity:0;

    transform:translateY(45px);

}

/* Reveal */

.statistics.visible .stat{

    opacity:1;

    transform:none;

}

.statistics.visible .stat:nth-child(1){

    transition-delay:.1s;

}

.statistics.visible .stat:nth-child(2){

    transition-delay:.25s;

}

.statistics.visible .stat:nth-child(3){

    transition-delay:.4s;

}

.statistics.visible .stat:nth-child(4){

    transition-delay:.55s;

}

/* Hover */

.stat:hover{

    transform:translateY(-10px);

    border-color:rgba(255,105,0,.45);

    background:rgba(255,255,255,.06);

    box-shadow:

        0 20px 50px rgba(0,0,0,.18);

}

/* Top Accent */

.stat::before{

    content:"";

    position:absolute;

    left:0;

    top:0;

    width:100%;

    height:3px;

    background:linear-gradient(

        90deg,

        transparent,

        var(--accent),

        transparent

    );

    opacity:.65;

}

/* -------------------------------- */

.stats-grid h2{

    position:relative;

    margin:0;

    font-size:68px;

    font-weight:700;

    line-height:1;

    color:var(--accent);

    letter-spacing:-2px;

}

/* Glow */

.stats-grid h2::after{

    content:"";

    position:absolute;

    width:140px;

    height:140px;

    left:50%;

    top:50%;

    transform:translate(-50%,-50%);

    background:

        radial-gradient(

            circle,

            rgba(255,105,0,.18),

            transparent 70%

        );

    z-index:-1;

}

/* -------------------------------- */

.counter{

    display:inline-block;

    min-width:100px;

}

.suffix{

    opacity:0;

    transform:translateY(8px);

    transition:

        opacity .4s ease,

        transform .4s ease;

}

.statistics.visible .suffix{

    opacity:1;

    transform:none;

    transition-delay:1.7s;

}

/* -------------------------------- */

.stats-grid p{

    margin-top:18px;

    color:rgba(255,255,255,.72);

    text-transform:uppercase;

    letter-spacing:3px;

    font-size:12px;

}

/* -------------------------------- */

@media(max-width:991px){

    .stats-grid{

        grid-template-columns:repeat(2,1fr);

    }

}

/* -------------------------------- */

@media(max-width:768px){

    .stats-grid{

        gap:20px;

    }

    .stat{

        padding:40px 20px;

    }

    .stats-grid h2{

        font-size:48px;

    }

    .counter{

        min-width:70px;

    }

}

/* -------------------------------- */

@media(max-width:560px){

    .stats-grid{

        grid-template-columns:1fr;

    }

}
</style>