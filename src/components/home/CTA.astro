<section class="cta section">
    <!-- Ambient Light Orbs for Backlight Depth -->
    <div class="ambient-glow orb-left"></div>
    <div class="ambient-glow orb-right"></div>

    <div class="container">
        <div class="cta-content">
            <span class="small-title">Start Your Project</span>
            
            <h2>
                Let's Create Something
                <br>
                <span class="text-shimmer">Extraordinary</span> Together.
            </h2>

            <p>
                Whether you're planning your dream home, commercial development,
                hospitality project or interior transformation, we'd love to
                hear your vision and help bring it to life.
            </p>

            <div class="cta-buttons">
                <a href="/contact" class="btn btn-primary">
                    <span class="btn-text">Schedule a Consultation</span>
                </a>

                <a href="/projects" class="btn btn-outline">
                    <span class="btn-text">View Our Projects</span>
                </a>
            </div>
        </div>
    </div>
</section>

<style>
/* --- Structure & Background Layers --- */
.cta {
    position: relative;
    background: #0a0a0a; /* True luxury deep black canvas */
    color: white;
    overflow: hidden;
    padding: 120px 0; /* Expanded padding for a sweeping editorial scale */
}

/* Luxury Fine Dot Grid Overlay */
.cta::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
        rgba(255, 255, 255, 0.08) 1px,
        transparent 1px
    );
    background-size: 40px 40px;
    opacity: 0.4;
    z-index: 2;
    pointer-events: none;
}

/* Atmospheric Ambient Lighting */
.ambient-glow {
    position: absolute;
    width: 600px;
    height: 600px;
    border-radius: 50%;
    filter: blur(140px);
    opacity: 0.12;
    z-index: 1;
    pointer-events: none;
    mix-blend-mode: screen;
    animation: pulseGlow 8s infinite alternate ease-in-out;
}

.orb-left {
    background: var(--accent, #c5a880); /* Warm golden light coming from bottom-left */
    bottom: -20%;
    left: -10%;
}

.orb-right {
    background: #ffffff; /* Soft moonlight coming from top-right */
    top: -20%;
    right: -10%;
    animation-delay: -4s;
}

@keyframes pulseGlow {
    0% { transform: scale(1) translate(0, 0); opacity: 0.08; }
    100% { transform: scale(1.15) translate(30px, -20px); opacity: 0.15; }
}

/* --- Content Typography --- */
.cta-content {
    position: relative;
    max-width: 850px;
    margin: auto;
    text-align: center;
    z-index: 3; /* Positions above background lighting paths */
}

.small-title {
    color: var(--accent, #c5a880);
    text-transform: uppercase;
    letter-spacing: 4px;
    font-size: 11px;
    font-weight: 500;
    display: inline-block;
    margin-bottom: 24px;
}

.cta h2 {
    color: white;
    font-size: clamp(2.5rem, 5vw, 3.8rem); /* Fluid modern scaling typography */
    font-weight: 300;
    line-height: 1.2;
    letter-spacing: -1px;
    margin: 0 0 35px 0;
}

/* Luxury Editorial Shimmer Text Effect */
.text-shimmer {
    font-weight: 400;
    background: linear-gradient(
        to right,
        #ffffff 20%,
        var(--accent, #c5a880) 50%,
        #ffffff 80%
    );
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: shineText 6s linear infinite;
}

@keyframes shineText {
    to { background-position: 200% center; }
}

.cta p {
    max-width: 660px;
    margin: auto;
    color: rgba(255, 255, 255, 0.6);
    font-size: 1.15rem;
    line-height: 1.7;
    font-weight: 300;
}

/* --- Premium Interactive Buttons --- */
.cta-buttons {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 24px;
    margin-top: 55px;
}

.btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 18px 38px;
    font-size: 13px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 2px;
    text-decoration: none;
    border-radius: 4px;
    overflow: hidden;
    transition: transform 0.3s cubic-bezier(0.25, 1, 0.5, 1),
                box-shadow 0.3s cubic-bezier(0.25, 1, 0.5, 1);
    transform: translateZ(0);
}

.btn:hover {
    transform: translateY(-3px);
}

/* Primary Button (Fluid Shimmer Effect) */
.btn-primary {
    background: #ffffff;
    color: #000000;
    box-shadow: 0 4px 20px rgba(255, 255, 255, 0.05);
}

.btn-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: -150%;
    width: 120%;
    height: 100%;
    background: linear-gradient(
        90deg,
        transparent,
        rgba(255, 255, 255, 0.6),
        transparent
    );
    transform: skewX(-20deg);
    transition: left 0.7s cubic-bezier(0.25, 1, 0.5, 1);
}

.btn-primary:hover {
    box-shadow: 0 15px 30px rgba(255, 255, 255, 0.15);
}

.btn-primary:hover::before {
    left: 150%;
}

/* Outline Button (Expanding Canvas Reveal) */
.btn-outline {
    background: transparent;
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.2);
    transition: border-color 0.5s ease, color 0.5s ease;
}

.btn-outline::before {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(255, 255, 255, 0.06);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.5s cubic-bezier(0.25, 1, 0.5, 1);
    z-index: 1;
}

.btn-outline:hover {
    border-color: rgba(255, 255, 255, 0.7);
}

.btn-outline:hover::before {
    transform: scaleX(1);
    transform-origin: left;
}

.btn-text {
    position: relative;
    z-index: 2;
}

/* --- Responsive Layout Rules --- */
@media(max-width: 768px) {
    .cta {
        padding: 90px 0;
    }

    .cta-buttons {
        flex-direction: column;
        gap: 16px;
        width: 100%;
        max-width: 320px;
        margin: 45px auto 0;
    }

    .btn {
        width: 100%;
        padding: 16px 24px;
    }

    .cta p {
        font-size: 1rem;
        line-height: 1.6;
    }
}
</style>